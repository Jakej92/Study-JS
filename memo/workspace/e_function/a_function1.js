// 함수의 선언
// f(x)  = 2x + 1

// function f(x) {
//     return 2 * x + 1;
// }

// console.log(f(3) + 9);

// // 두 정수의 곱셈을 구해주는 함수
// function multiply(number1, number2) {
//     let result = number1 * number2;
//     return result;
//     // return number1 * number1;
// }

// let result = multiply(10, 2);
// console.log(result);

// 두 정수의 곰셈을 구해주는 함수

// function multiply(number1, number2) {
//     return number1 * number2;
// }
// let result = multiply(2, 7);
// console.log(result);

// // 두 정수의 나눗셈을 통해 결과값을 구한다.
// // 단, 분모는 0이 될 수 없다.
// // 만약, 분모가 0이라면 결과값은 0으로 약속한다.

// function divide(number1, number2) {
//     if (number2 === 0) {
//         console.log(number2);
//     }
//     let result = number1 / number2;
//     return result;
// }
// let result = divide(10, 0);
// console.log(result);

// 두 정수의 나눗셈을 통해 결과값을 구한다.
// 단, 분모는 0이 될 수 없다.
// 만약, 분모가 0이라면 결과값은 0으로 약속한다.

// function divide(number1, number2) {
//     // if (number2 === 0) {
//     //     //return을 만나면, 함수는 즉시 종료된다.
//     //     return 0;
//     // }

//     // return number1 / number2;
//     return number2 === 0 ? 0 : number1 / number2;
// }

// let result = divide(10, 0);
// console.log(result);

// 1 ~ 10까지 출력하는 함수
function print1to10
for (i = 0; i < 10; i = i + 1) {
    console.log(i + 1);
}
// "홍길동"을 n번 출력하는 함수
function printHong(count) {
    for(let i=)
}
// 이름을 n번 출력하는 함수
function printName(name, count) {
    for(let i=0; i < count; i++) {
        console.log(name);
    }
}
printName(정도영, 20);

// 세 정수의 뺄셈 함수
function remove(number1, number2, number3) {
    let result = number1 - number2 - number3;
    return result;
}
let result = remove(10, 4, 1);
console.log(result);
// 1 ~ n까지의 합을 구해주는 함수


let total = 0;
for (i = 0; i < 10; i++) {
    total += i + 1;
}
console.log(total);
// 홀수를 짝수로, 짝수를 홀수로 바꿔주는 함수
function change(number) {
    return ++number;
}
console.log(number+1);
